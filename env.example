# Copy this file to ".env" and fill in your REAL password.
# Never upload the ".env" file itself to GitHub (it's already excluded in .gitignore).

DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/nepa_unilag_csc316_db
